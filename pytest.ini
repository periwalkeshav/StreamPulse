[pytest]
testpaths = tests
pythonpath = . producer consumer
addopts = -q --strict-markers --color=yes
markers =
    integration: requires the full docker-compose stack (set RUN_INTEGRATION=1)
filterwarnings =
    ignore::DeprecationWarning
